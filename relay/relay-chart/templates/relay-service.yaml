apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.serviceName }}
spec:
  ports:
    - name: relay
      port: {{ .Values.service.port }}
      protocol: TCP
      targetPort: {{ .Values.serviceport }}
  selector:
    app: "{{ template "relay.fullname" . }}-ist"
